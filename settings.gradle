pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://mvn.tetratau.net/releases' }
    }
}

rootProject.name = 'advancedmanhunt'
include ':injector'
project(':injector').projectDir = new File(settingsDir, 'injector')
